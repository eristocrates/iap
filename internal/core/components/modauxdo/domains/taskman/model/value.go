package taskman

import "github.com/google/uuid"

// todo break into files in relevant values folder
type taskId struct {
	value int
}
type id struct {
	value uuid.UUID
}
type name struct {
	value string
}
