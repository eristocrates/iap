lazy nushell start
go run (ls **/*.go | get name | str join " ")

graphviz run hack
go run main.go | save -f  /mnt/c/Users/eristocrates/notes/test.dot

scheduler (reference actual calendars like outlook and gcalendar)
prioritization decoupled from tasks
seperate but related quantifying, qualifying, and quantizing operations. maybe standardize too? if so, it's certainly related to bulk operations

operations, operands, and arity
self qualifying, self quantifying, prioritization metric quantizing and standardizing, schedule operation on a task is when it should be completed, but a scheduling operation on a bill is when the money is due

at somepoint i need to build a thought relation engine, to enable fully featured gui assembly of highly customized use of the program

absolutely commit the ui curse of a screen with literally every button on one screen. reference modular synthisizers somehow (modular synthinkizer?)

any giving thing has it's definition, it's internal name, and a customizable alias so user's are not stuck with whatever metaphor I choose to implement

calendar events by universal time codes, as daylight savings time messed up my radiko reminders

represent a train of thoughts somehow. 